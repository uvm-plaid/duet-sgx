let main = pλ ε  : ℝ⁺,
              δ  : ℝ⁺
              .
              ε  : ℝ⁺[ε],
              δ  : ℝ⁺[δ],
              df : 𝕄 [L∞ , U | ★ , 𝐝 ℕ ∷ 𝐝 ℕ ∷ [] ]
              ⇒
  gauss[ℝ⁺[1.0], ε, δ] <df> {
    real (rows (mfilter df { x ⇒ x#[ℕ[0], ℕ[0]] ≡ disc 1 })) }
in main
